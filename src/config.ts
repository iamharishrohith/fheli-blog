export const FIELDID_API_ENDPOINT = 'https://api.datalake3.nhai.gov.in/v1/fieldid-sync';

// Replace this value during deployment with a real Cognito/JWT device token.
// Keeping it empty prevents accidental uploads with a placeholder credential.
export const FIELDID_SYNC_AUTH_TOKEN = '';
